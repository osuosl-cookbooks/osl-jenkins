credentials:
  system:
    domainCredentials:
    - credentials:
      - usernamePassword:
          id: "<%= @name %>"
          password: ${file:casc_configs/secrets/password_credentials_password_<%= @name %>}
          scope: <%= @scope %>
          username: ${file:casc_configs/secrets/password_credentials_username_<%= @name %>}
