unclassified:
  globalLibraries:
    libraries:
      - name: osl-pipelines
        defaultVersion: <%= @branch %>
        implicit: false
        allowVersionOverride: false
        retriever:
          modernSCM:
            scm:
              git:
                remote: <%= @repo %>
                credentialsId: cookbook_uploader
