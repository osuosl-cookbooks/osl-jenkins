unclassified:
  gitLabServers:
    servers:
      - name: <%= @name %>
        serverUrl: <%= @url %>
        credentialsId: <%= @credential_id %>
        manageWebHooks: true
        manageSystemHooks: false
